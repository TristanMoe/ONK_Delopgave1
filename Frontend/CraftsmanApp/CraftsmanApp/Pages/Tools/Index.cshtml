@page
@model CraftsmanApp.Pages.Tools.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tool[0].Purchased)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tool[0].Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tool[0].Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tool[0].Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tool[0].SerialNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Tool) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Purchased)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SerialNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ToolId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ToolId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ToolId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
